// Highlight plugin types

export interface HighlightMarkAttrs {
  // No additional attributes needed for basic highlight
}

export interface SuperscriptMarkAttrs {
  // No additional attributes needed
}

export interface SubscriptMarkAttrs {
  // No additional attributes needed
}
